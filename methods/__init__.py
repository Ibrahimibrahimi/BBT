# This file intentionally left mostly empty.
# The `methods` package is scanned dynamically by loader.py —
# just drop a new .py file in here that defines a BaseMethod subclass
# and it will be picked up automatically, no import needed here.
